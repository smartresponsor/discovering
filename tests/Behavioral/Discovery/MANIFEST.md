# Behavioral Discovery Test Manifest

Use this bucket for scenario-level behavior across several services, such as feedback learning and mode behavior.

Behavioral tests can coordinate multiple collaborators, but they should still avoid HTTP/container concerns unless the behavior is explicitly exposed through the Symfony surface.
