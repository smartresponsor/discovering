# Discovery Document Interface Manifest

Document contracts define provider surfaces that return immutable discovery documents.

Rules:

- Providers expose product-oriented document supply behavior.
- Interfaces must end with `Interface`.
- Implementations belong under `src/Service/Discovery/Document` or an existing service capability folder when already established.
