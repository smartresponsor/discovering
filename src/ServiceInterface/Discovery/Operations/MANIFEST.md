# Discovery Operations Interface Manifest

Operations contracts define event-log behavior for discovery operational actions.

Rules:

- Contracts must describe operation/event-log behavior, not storage technology.
- Interfaces must end with `Interface`.
- Implementations belong under `src/Service/Discovery/Operations`.
