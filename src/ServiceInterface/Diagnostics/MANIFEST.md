# Discovery Diagnostics Interface Manifest

Diagnostics contracts define probe and reachability boundaries for discovery runtime health surfaces.

Rules:

- Interfaces must end with `Interface`.
- Interfaces remain under `App\ServiceInterface\Discovery\Diagnostics`.
- Implementations belong under `src/Service/Discovery/Diagnostics`.
